/** @type {import('next').NextConfig} */
const nextConfig = {
    // Allowing all domains  to be used for CORS. This may need to be adjusted depending on your specific use case
  reactStrictMode: true,
  poweredBy: false,
   
};

export default nextConfig;
